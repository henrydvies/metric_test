# Jellyfaas ignore file.
# These are files that will not be added in to the zip on build/deploy.
# Uses the standard .gitignore format. https://git-scm.com/docs/gitignore/en

*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/